---
export interface Props {
  href: string;
  text: string;
  key?: string | number; // Propiedad opcional para 'key'
}
const { href, text } = Astro.props;
---
<li>
  <a href={href} class="text-gray-800 dark:text-white hover:text-primary">
    {text}
  </a>
</li>
